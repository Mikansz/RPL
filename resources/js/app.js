import './bootstrap';
import { createApp } from 'vue';

// Create Vue app instance
const app = createApp({});

// Global components can be registered here
// app.component('example-component', ExampleComponent);

// Mount the app
app.mount('#app');
